node_modules/
dist/

# macOS
.DS_Store
.env
.env.*
!.env.example
.mcp.json
*.pem
*.key
data/audit/*.jsonl
data/state/*.txt
~$*

# Local-only working notes / reports — never committed
report/

# Org-specific manifest overlay — overrides manifests/core/ during setup, never shipped
manifests/local/

# Scratch/test fixtures — never commit
scratch-*
